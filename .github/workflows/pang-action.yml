name: GitHub Actions Demo #! ชื่อ โครงการ
on: [push]
jobs:
  Explore-GitHub-Actions: #! ชื่อ job
    runs-on: ubuntu-latest #! os ที่ใช้งาน
    steps:
      - run: echo "🎉 Hello Pang" #! รันคำสั่ง ubuntu ให้ echo ออกมา
      -run curl -X POST -H 'Authorization: Bearer sbgUpn2UGSCWs81res3LO3urNXdrCATNmndhuxjboat' -F 'message=Pang อัปงานเรียบร้อย' https://notify-api.line.me/api/notify
